export interface PlaygroundFiles {
  /** JavaScript/TypeScript code shown in the JS tab */
  js: string;
  /** HTML markup shown in the HTML tab and rendered in the iframe preview */
  html: string;
}
